#!/bin/sh

test -x <%= @msktutilpath %> || exit 0

# These options are overridden in /etc/defaults/msktutil.
# Edit there, not here.
AUTOUPDATE_ENABLED="false"
AUTOUPDATE_OPTIONS=""

[ -r /etc/defaults/msktutil ] && . /etc/defaults/msktutil

[ "$AUTOUPDATE_ENABLED" = "true" ] || exit 0
exec <%= @msktutilpath %> --auto-update $AUTOUPDATE_OPTIONS
